<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LoginDialog" parent="@android:style/Theme.Holo.DialogWhenLarge">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--自定义等待view中的属性-->
    <declare-styleable name="CustomLoadingView">
        <!--loading动画执行一次所需的时间-->
        <attr name="round_once_time" format="integer"></attr>
        <!--外层圆弧的颜色-->
        <attr name="round_outside_color" format="color"></attr>
        <!--内层圆弧的颜色-->
        <attr name="round_inside_color" format="color"></attr>
        <!--内层圆弧的角度-->
        <attr name="round_inside_angle" format="float"></attr>
        <!--外层圆弧的角度默认为330度-->
        <attr name="round_outside_angle" format="float"></attr>
        <!--圆弧旋转的起始角度-->
        <attr name="round_start_angle" format="float"></attr>
    </declare-styleable>

    <declare-styleable name="MPEmptyView">
        <attr name="imgEmpty" format="reference" />
        <attr name="imgError" format="reference" />
        <attr name="textEmpty" format="string" />
        <attr name="textError" format="string" />
        <attr name="textColor" format="reference" />
    </declare-styleable>
</resources>